# TR1894 syntax schema

`tr1894-syntax.schema.json` validates the proposed canonical nested research-document shape in `NT_SCHEMA.md`. It does not validate a complete corpus, and no dataset instance has been created.

JSON Schema validates local object shape. A companion semantic validator is still required for source-token uniqueness, graph acyclicity, single parentage, complete terminal coverage, and agreement between clause index and tree.

