# Finished

Finished is only in `STATUS.md`. This app may record a named human's explicit
approval for a stage already marked `ready`; it never infers `done`.
See `DATA_CONTRACT.md` and `WORKFLOW.md` at the repository root.
