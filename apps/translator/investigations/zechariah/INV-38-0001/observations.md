# Observations

These are mechanically extracted facts, not conclusions.

- The controlled OSHB XML contains six direct words marked `type="x-ketiv"` followed by `note type="variant"` with a nested `rdg type="x-qere"` word.
- The current spine contains all six ketiv tokens and none of the six qere tokens.
- Every controlled spine token otherwise maps exactly and uniquely to its OSHB identifier.
- The six qere tokens are the only rows in the available Zacarías interlinear dataset that do not appear in the direct-word spine.
- The affected references are Zechariah 1:4; 1:16; 4:2; 11:2; 14:2; and 14:6.
- No human decision is recorded for these variants.
- WORKFLOW v1.1 records that unused qere is not a translator vote. This inventory is documentation, not a release-blocking source-policy question.

