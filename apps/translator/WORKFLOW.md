# Translator workflow

The project workflow is [`WORKFLOW.md`](../../WORKFLOW.md).

This application edits `translation/` and `alignment/`.
It does not approve books. It does not keep a second corpus.
